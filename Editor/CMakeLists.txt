# Editor target placeholder.
# The editor will be implemented in a future phase.
# This file establishes the build target so the project structure is complete.

message(STATUS "Editor build is not yet implemented. Skipping.")